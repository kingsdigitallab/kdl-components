{% extends "../../component.njk" %}

{% block component %}
<div class="kdl-header:title">
  <a class="kdl-header:link" href="{{ kwargs.url | default('/') }}">{{ kwargs.title }}</a>
</div>
{% if kwargs.logo %}
<div class="kdl-header:logo">
  <img src="{{ kwargs.logo.src }}" alt="{{ kwargs.logo.alt }}">
</div>
{% endif %}
{% endblock %}
